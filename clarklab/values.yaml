jupyterhub:
  hub:
    labels:
      app: jupyterhub
    resources: 
      requests:
        memory: "1000Mi"
        cpu: "100m"
      limits:
        memory: "2000Mi"
        cpu: "200m"
  proxy:
    labels:
      app: jupyterhub
    chp:
      resources:
        requests:
          memory: "1000Mi"
          cpu: "100m"
        limits:
          memory: "2000Mi"
          cpu: "200m"
    service:
      type: ClusterIP
  prePuller:
    hook:
      enabled: false
    labels:
      app: jupyterhub
    resources:
      requests:
        memory: "1000Mi"
        cpu: "100m"
      limits:
        memory: "2000Mi"
        cpu: "200m"
  scheduling:
    userPlaceholder:
      resources:
        requests:
          memory: "1000Mi"
          cpu: "100m"
        limits:
          memory: "2000Mi"
          cpu: "200m"
    