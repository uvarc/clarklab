apiVersion: v1
kind: Service
metadata:
  name: minio-frontend-service
  namespace: clarklab
spec:
  selector:
    app: jupyterhub
  ports:
  - name: frontend
    protocol: TCP
    port: 80
    targetPort: 9001
